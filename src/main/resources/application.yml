spring:
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
      table-name: spring_session
  datasource:
    url: jdbc:postgresql://localhost:5432/aviasales
    username: roflandown
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
